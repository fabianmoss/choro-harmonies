name: harmony # my env + dependencies i had to install
channels:
  - defaults
dependencies:
  - python=3.10
  - pytorch=2.0
  - torchvision
  - torchaudio
  - ipykernel
prefix: /opt/miniconda3/envs/harmony
